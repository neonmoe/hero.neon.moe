html
  head
  title #{title}
  style
    include style.css
  body
    h1 #{header}
    p #{description}
    h3 List of all characters that exist on the current world:
    p #{charlist}

    if authorized
      p You're authorized! Visit /a to view your auth token.
    else
      button(onclick="genuser()") Generate new user

    <script>
    include index.js
    </script>
